package simulador_escalonador;
import java.lang.Math;

public class Memory {

    //32 * 2^30 

    private class PageInfo{

    }

    static long FrameSize = Math.round(Math.pow(2, 30));

    PageInfo frames[] = new PageInfo[32]; 


    
}
